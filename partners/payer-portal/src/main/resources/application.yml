spring:
  application:
    name: clearpath-payer-portal
  jackson:
    default-property-inclusion: non_null

server:
  port: ${PORT:8084}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
